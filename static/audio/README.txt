Record a short voice message (20-40 seconds works well — phone voice
memos are fine) and save/export it as an MP3 named exactly:

  voice-note.mp3

Put it in this folder. It will show up as a small audio player on the
final page automatically. If you don't add a file, the player just
won't appear — nothing will break.
